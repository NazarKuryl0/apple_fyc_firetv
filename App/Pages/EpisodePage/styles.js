import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    flex: 1,
  },
  videoBlock: {
    width: '100%',
    height: '100%',
  },
  wmBlock: {
    position: 'absolute',
    top: 100,
    left: 100,
    zIndex: 1,
  },
});
